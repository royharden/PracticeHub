{
  "synthetic": true,
  "railId": "RAIL-016",
  "class": "FAILURE",
  "description": "compendium-sim: the licensed content cannot be read at all, which disables every command that depends on it.",
  "cases": [
    {
      "op": "arm",
      "name": "arm X-16",
      "primitiveId": "X-16"
    },
    {
      "op": "dispatch",
      "name": "the request fails without landing a clean effect",
      "request": {
        "operation": "fetch-edition",
        "idempotencyKey": "synthetic-rail-016-failure",
        "payloadRef": "synthetic-payload-rail-016-failure",
        "requestedAt": "2026-01-01T00:00:00Z",
        "payload": {
          "synthetic": true,
          "ref": "synthetic-body-failure"
        }
      },
      "expect": {
        "status": "unavailable",
        "effectState": "not-landed",
        "receiptRef": "absent",
        "requiresReconciliation": false
      }
    },
    {
      "op": "drain-receipts",
      "name": "no receipt is emitted for a request that carried nothing",
      "expect": {
        "kinds": []
      }
    },
    {
      "op": "snapshot",
      "name": "the ledger records the attempt and its state, never a phantom success",
      "expect": {
        "effects": 1,
        "receipts": 0,
        "effectState": "not-landed"
      }
    },
    {
      "op": "record-heartbeat",
      "name": "the rail is alive and answering — it simply carried nothing",
      "at": "2026-01-01T00:01:00Z"
    },
    {
      "op": "heartbeat",
      "name": "a rail that answers and carries nothing is loud, not calm",
      "expect": {
        "verdict": "alarm",
        "reasons": ["volume-below-band"]
      }
    }
  ]
}
