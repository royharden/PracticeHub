{
  "synthetic": true,
  "railId": "RAIL-011",
  "class": "FAILURE",
  "description": "lab-sim: the interface answers in a shape its own contract forbids; nothing may be filed from an unparseable result.",
  "cases": [
    {
      "op": "arm",
      "name": "arm X-17",
      "primitiveId": "X-17"
    },
    {
      "op": "dispatch",
      "name": "the request fails without landing a clean effect",
      "request": {
        "operation": "place-order",
        "idempotencyKey": "synthetic-rail-011-failure",
        "payloadRef": "synthetic-payload-rail-011-failure",
        "requestedAt": "2026-01-01T00:00:00Z",
        "payload": {
          "synthetic": true,
          "ref": "synthetic-body-failure"
        }
      },
      "expect": {
        "status": "malformed",
        "effectState": "unknown",
        "receiptRef": "absent",
        "requiresReconciliation": true
      }
    },
    {
      "op": "drain-receipts",
      "name": "no receipt is emitted for a request that carried nothing",
      "expect": {
        "kinds": []
      }
    },
    {
      "op": "snapshot",
      "name": "the ledger records the attempt and its state, never a phantom success",
      "expect": {
        "effects": 1,
        "receipts": 0,
        "effectState": "unknown"
      }
    },
    {
      "op": "record-heartbeat",
      "name": "the rail is alive and answering — it simply carried nothing",
      "at": "2026-01-01T00:01:00Z"
    },
    {
      "op": "heartbeat",
      "name": "a rail that answers and carries nothing is loud, not calm",
      "expect": {
        "verdict": "alarm",
        "reasons": ["volume-below-band"]
      }
    }
  ]
}
