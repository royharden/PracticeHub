{
  "synthetic": true,
  "railId": "RAIL-013",
  "class": "BOUNDARY",
  "description": "idproof-sim-rail: the exact edge — the proofing attempt ends uncertain — an unresolved assertion is never a verified relationship.",
  "cases": [
    {
      "op": "arm",
      "name": "arm X-05 for exactly one request",
      "primitiveId": "X-05",
      "options": {
        "count": 1
      }
    },
    {
      "op": "dispatch",
      "name": "the armed X-05 shapes the first request",
      "request": {
        "operation": "start-proofing",
        "idempotencyKey": "synthetic-rail-013-boundary",
        "payloadRef": "synthetic-payload-rail-013-boundary",
        "requestedAt": "2026-01-01T00:00:00Z",
        "payload": {
          "synthetic": true,
          "ref": "synthetic-body-boundary"
        }
      },
      "expect": {
        "status": "uncertain",
        "effectState": "unknown",
        "receiptRef": "absent",
        "requiresReconciliation": true
      }
    },
    {
      "op": "drain-receipts",
      "name": "the receipts the rail actually emitted, in drain order",
      "expect": {
        "kinds": []
      }
    },
    {
      "op": "dispatch",
      "name": "the very next attempt on the same key adds no second external effect",
      "request": {
        "operation": "start-proofing",
        "idempotencyKey": "synthetic-rail-013-boundary",
        "payloadRef": "synthetic-payload-rail-013-boundary",
        "requestedAt": "2026-01-01T00:01:00Z",
        "payload": {
          "synthetic": true,
          "ref": "synthetic-body-boundary"
        }
      },
      "expect": {
        "attempts": 2,
        "resendsExternalEffect": false
      }
    },
    {
      "op": "snapshot",
      "name": "still exactly one ledger record for the key",
      "expect": {
        "effects": 1
      }
    }
  ]
}
