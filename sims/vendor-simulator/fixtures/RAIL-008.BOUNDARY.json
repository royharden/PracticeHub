{
  "synthetic": true,
  "railId": "RAIL-008",
  "class": "BOUNDARY",
  "description": "stripe-sim: the exact edge — the money moves and the webhook never lands: a landed effect WITHOUT a receipt is not a licence to send again.",
  "cases": [
    {
      "op": "arm",
      "name": "arm X-06 for exactly one request",
      "primitiveId": "X-06",
      "options": {
        "count": 1
      }
    },
    {
      "op": "dispatch",
      "name": "the armed X-06 shapes the first request",
      "request": {
        "operation": "create-payment-intent",
        "idempotencyKey": "synthetic-rail-008-boundary",
        "payloadRef": "synthetic-payload-rail-008-boundary",
        "requestedAt": "2026-01-01T00:00:00Z",
        "payload": {
          "synthetic": true,
          "ref": "synthetic-body-boundary"
        }
      },
      "expect": {
        "status": "accepted",
        "effectState": "landed",
        "receiptRef": "absent",
        "requiresReconciliation": true
      }
    },
    {
      "op": "drain-receipts",
      "name": "the webhook gap means no receipt at all",
      "expect": {
        "kinds": []
      }
    },
    {
      "op": "dispatch",
      "name": "the landed effect still dedupes on the very next attempt",
      "request": {
        "operation": "create-payment-intent",
        "idempotencyKey": "synthetic-rail-008-boundary",
        "payloadRef": "synthetic-payload-rail-008-boundary",
        "requestedAt": "2026-01-01T00:01:00Z",
        "payload": {
          "synthetic": true,
          "ref": "synthetic-body-boundary"
        }
      },
      "expect": {
        "status": "deduplicated",
        "requiresReconciliation": true,
        "attempts": 2
      }
    },
    {
      "op": "snapshot",
      "name": "still exactly one ledger record for the key",
      "expect": {
        "effects": 1
      }
    }
  ]
}
