{
  "synthetic": true,
  "requirementId": "REQ-ID-008",
  "class": "HAPPY",
  "description": "Scoped caregiver access: the patient-configured scope drives every decision, and every proxy action attributes the proxy actor.",
  "cases": [
    {
      "name": "a scoped scheduling grant lets the caregiver book without full chart access (AC-4/AC-7)",
      "op": "evaluate",
      "evaluate": {
        "actor": { "kind": "proxy" },
        "segment": "scheduling",
        "action": "edit",
        "subject": "minor",
        "stepUp": true,
        "patientState": "NV"
      },
      "expectAllowed": true
    },
    {
      "name": "the audit trail records the proxy as the actor, never the patient (AC-9)",
      "op": "evaluate",
      "evaluate": {
        "actor": { "kind": "proxy" },
        "segment": "messaging",
        "action": "edit",
        "subject": "minor",
        "stepUp": true,
        "patientState": "NV"
      },
      "expectAllowed": true,
      "expectActorRef": "synthetic-portal:np-alex-rivera"
    }
  ]
}
